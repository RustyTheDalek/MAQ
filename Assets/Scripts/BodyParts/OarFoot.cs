using UnityEngine;
using System.Collections;

public class OarFoot : LegJoint {

	// Use this for initialization
    protected void Start()
    {
	}
	
	// Update is called once per frame
    protected void Update()
    {
	    
        //if(testing && canRace)
        //{
        //    movement.z = -Input.GetAxis ("LeftJoystickX_P" + playerID);
        //    movement.y = Input.GetAxis ("LeftJoystickY_P" + playerID);

        //    rigidbody.AddForce(Vector3.up * movement.y * walkForce * Time.deltaTime * transform.root.localScale.x*2);

        //    rigidbody.AddForce(transform.root.forward * movement.z * walkForce * Time.deltaTime * transform.root.localScale.x*2);
        //}
	}
}